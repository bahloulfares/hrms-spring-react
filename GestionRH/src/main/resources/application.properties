# JWT Configuration
jwt.secret=${JWT_SECRET:MyVerySecureJWTSecretKeyThatIsAtLeast256BitsLongForHMACOperationsAndMeetsRFC7518Requirements1234}
jwt.expiration=86400000

# Server Configuration
server.port=${SERVER_PORT:8088}

# Database MySQL (XAMPP)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/gestionrh?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=Africa/Tunis&allowPublicKeyRetrieval=true&sslMode=DISABLED}
spring.datasource.username=${DB_USER:gestionrh_app}
spring.datasource.password=${DB_PASSWORD:Re88o830u3*}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# Note: Hibernate 7+ automatically detects the dialect. 
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.globally_quoted_identifiers=true
spring.jpa.properties.hibernate.jdbc.time_zone=Africa/Tunis
spring.jpa.open-in-view=false

# Durée d'une journée de travail (conversion heures -> jours)
app.workday.hours=8

# Notifications (channels off by default)
notification.email.enabled=${NOTIFICATION_EMAIL_ENABLED:true}
notification.email.from=${NOTIFICATION_EMAIL_FROM:bahloulfares3@gmail.com}
notification.email.to=${NOTIFICATION_EMAIL_TO:bahloulfares36@gmail.com,naturolivart@gmail.com}
notification.slack.enabled=${NOTIFICATION_SLACK_ENABLED:false}
notification.slack.webhook-url=${NOTIFICATION_SLACK_WEBHOOK:}
notification.sms.enabled=${NOTIFICATION_SMS_ENABLED:false}
notification.sms.webhook-url=${NOTIFICATION_SMS_WEBHOOK:}
notification.sms.to=${NOTIFICATION_SMS_TO:}

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:bahloulfares3@gmail.com}
spring.mail.password=${MAIL_PASSWORD:nifm vywc rzvg gtcu}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Flyway (active si dépendance présente)
# Spring Boot properties must be prefixed with 'spring.flyway.*'
# Flyway
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=false
spring.flyway.locations=classpath:db/migration
spring.flyway.url=${DB_URL:jdbc:mysql://localhost:3306/gestionrh?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=Africa/Tunis&allowPublicKeyRetrieval=true&sslMode=DISABLED}
spring.flyway.user=${DB_USER:gestionrh_app}
spring.flyway.password=${DB_PASSWORD:Re88o830u3*}
spring.flyway.validate-on-migrate=true
spring.flyway.out-of-order=false
spring.flyway.repair-on-migrate=false

# Database H2 (Backup)
# spring.datasource.url=jdbc:h2:mem:gestionrh
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# Logging
logging.level.root=INFO
logging.level.com.fares.gestionrh=DEBUG
logging.level.org.springframework.security=WARN
logging.level.org.flywaydb=INFO

# SpringDoc OpenAPI / Swagger Configuration
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.operations-sorter=method
springdoc.swagger-ui.tags-sorter=alpha
springdoc.swagger-ui.tryItOutEnabled=true
springdoc.swagger-ui.displayOperationId=true
# Show request/response examples in Swagger UI
springdoc.swagger-ui.show-common-extensions=true


# ============================================================================
# ENVIRONMENT VARIABLES - BACKEND (Spring Boot)
# Copy to GestionRH/.env and fill with your values. Do NOT commit .env.
# Supports both dev profile (MySQL) and prod profile (PostgreSQL).
# ============================================================================

# --------------------------------------------------------------------------
# DEV PROFILE (application.properties) - MySQL
# --------------------------------------------------------------------------
DB_URL=jdbc:mysql://localhost:3306/gestionrh?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=Africa/Tunis&allowPublicKeyRetrieval=true&sslMode=DISABLED
DB_USER=gestionrh_app
DB_PASSWORD=Re88o830u3*
SERVER_PORT=8088

# Mail (dev defaults are fine; override as needed)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=bahloulfares3@gmail.com
MAIL_PASSWORD=nifm vywc rzvg gtcu

# Notifications (channels off by default)
NOTIFICATION_EMAIL_ENABLED=true
NOTIFICATION_EMAIL_FROM=bahloulfares3@gmail.com
NOTIFICATION_EMAIL_TO=bahloulfares36@gmail.com,naturolivart@gmail.com
NOTIFICATION_SLACK_ENABLED=false
NOTIFICATION_SLACK_WEBHOOK=
NOTIFICATION_SMS_ENABLED=false
NOTIFICATION_SMS_WEBHOOK=
NOTIFICATION_SMS_TO=

# JWT (IMPORTANT: Must be at least 256 bits / 32 bytes for HMAC-SHA algorithms)
# Generate securely: 
#   Windows PowerShell: [Convert]::ToBase64String((1..32 | % {Get-Random -Min 0 -Max 256}))
#   Linux/Mac: openssl rand -base64 32
JWT_SECRET=MyVerySecureJWTSecretKeyThatIsAtLeast256BitsLongForHMACOperationsAndMeetsRFC7518Requirements1234
JWT_EXPIRATION=86400000

# --------------------------------------------------------------------------
# PROD PROFILE (application-prod.properties) - PostgreSQL
# --------------------------------------------------------------------------
DATABASE_URL=jdbc:postgresql://prod-db-server:5432/gestionrh
DATABASE_USERNAME=gestionrh_user
DATABASE_PASSWORD=your_secure_password_here

# JWT
# Generate securely: openssl rand -base64 32
# JWT_SECRET=your-256-bit-secret-for-prod
# JWT_EXPIRATION=86400000

# Logging
LOG_FILE_PATH=/var/log/gestionrh/application.log

# CORS
CORS_ALLOWED_ORIGINS=https://app.yourdomain.com,https://www.yourdomain.com

# --------------------------------------------------------------------------
# NOTES
# --------------------------------------------------------------------------
# - Never commit .env (already ignored by .gitignore).
# - Keep JWT_SECRET unique per environment (256-bit minimum).
# - Use DATABASE_* for the prod profile; use DB_* for the dev profile.

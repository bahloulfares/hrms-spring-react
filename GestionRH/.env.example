# ==================================================
# VARIABLES D'ENVIRONNEMENT - HRMS Application
# ==================================================
# 
# Instructions:
# 1. Copier ce fichier en ".env" (git ignored)
# 2. Remplacer les valeurs par vos paramètres
# 3. Charger via: export $(cat .env | xargs)
#
# ==================================================

# DÉVELOPPEMENT LOCAL (application-dev.properties)
# ==================================================
DATABASE_URL=jdbc:mysql://localhost:3306/gestionrh?createDatabaseIfNotExist=true&serverTimezone=Africa/Tunis
DATABASE_USERNAME=root
DATABASE_PASSWORD=
JWT_SECRET=VotreCleSecreteSuperSecuriseePourJWTMinimum256BitsDeSecurite2024
JWT_EXPIRATION=86400000

# PRODUCTION (application-prod.properties)
# ==================================================
# DATABASE - PostgreSQL (recommandé en prod)
# DATABASE_URL=jdbc:postgresql://prod-db-server:5432/gestionrh
# DATABASE_USERNAME=gestionrh_user
# DATABASE_PASSWORD=your_secure_password_here

# JWT - À générer avec une valeur sécurisée (min 256 bits)
# Générer: openssl rand -base64 32
# JWT_SECRET=your_random_256_bit_secret_key_generated_securely
# JWT_EXPIRATION=86400000

# SERVER
# SERVER_PORT=8088

# LOGGING
# LOG_FILE_PATH=/var/log/gestionrh/application.log

# CORS - Origines autorisées en production
# CORS_ALLOWED_ORIGINS=https://app.yourdomain.com,https://www.yourdomain.com

# ==================================================
# NOTES IMPORTANTES
# ==================================================
# 
# 🔐 SÉCURITÉ:
# - Ne JAMAIS commiter le fichier .env en git
# - JWT_SECRET doit être unique et complexe (min 256 bits)
# - DATABASE_PASSWORD doit être sécurisé
# 
# 🌐 DÉPLOIEMENT DOCKER:
# Ces variables peuvent être passées via:
# - docker run -e DATABASE_URL=... -e JWT_SECRET=...
# - docker-compose.yml (non recommandé)
# - Kubernetes Secrets
#
# 📋 VARIABLES OPTIONNELLES:
# Si non défini, utilise les valeurs par défaut du application.properties
#
